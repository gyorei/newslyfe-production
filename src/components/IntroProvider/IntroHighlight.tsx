// src/components/IntroProvider/IntroHighlight.tsx
import React from 'react';

// Minimális placeholder highlight overlay
export const IntroHighlight: React.FC<{ targetId?: string }> = ({ targetId }) => {
  // Később: overlay/highlight logika
  return null;
};
