// src/components/IntroProvider/IntroPointer.tsx
import React from 'react';

// Minimális placeholder komponens, később animációval
export const IntroPointer: React.FC<{ x?: number; y?: number }> = ({ x = 0, y = 0 }) => {
  // Később: abszolút pozícionált mutató
  return null;
};
